# Copy this file to `.env` and fill in your DEV database connection string.
# NEVER commit the real `.env` file.

# Example (Neon/Supabase dev). Ensure SSL params as required by your provider.
DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require

# JWT secret for signing tokens - use a long random string in production
JWT_SECRET=change-this-to-a-random-secure-string

# Optional: reference only. Set this in your hosting provider, not locally.
# DATABASE_URL_PROD=postgres://user:password@host:5432/database?sslmode=require
